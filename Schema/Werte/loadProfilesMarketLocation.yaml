components:
  schemas:
    loadProfilesMarketLocation:
      type: array
      items:
        $ref: './loadProfileMarketLocation.yaml#/components/schemas/loadProfileMarketLocation'
      minItems: 1
      maxItems: 40000
