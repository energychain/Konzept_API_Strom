components:
  schemas:
    loadProfilesNetLocation:
      type: array
      items:
        $ref: './loadProfileNetLocation.yaml#/components/schemas/loadProfileNetLocation'
      minItems: 1
      maxItems: 40000
