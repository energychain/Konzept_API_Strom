components:
  schemas:
    courtData:
      type: object
      description: Gericht und Handelsregister-Nummern
      required:
        - courtName
        - commercialRegisterNumber
      properties:
        courtName:
          $ref: './courtName.yaml#/components/schemas/courtName'
        commercialRegisterNumber:
          $ref: './commercialRegisterNumber.yaml#/components/schemas/commercialRegisterNumber'
        
