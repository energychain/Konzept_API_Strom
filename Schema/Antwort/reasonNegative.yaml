components:
  schemas:
    reasonNegative:
      type: string
      description: Grund des Scheiterns.
      example: communicationFailure
      enum:
      - communicationFailure
      - overload
      - unable
