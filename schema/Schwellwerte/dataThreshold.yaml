components:
  schemas:
    dataThreshold:
      required:
        - threshold
        - lowerValue
        - upperValue
      type: object
      properties:
        threshold:
          $ref: './threshold.yaml#/components/schemas/thresholt'
        lowerValue:
          $ref: './lowerValue.yaml#/components/schemas/lowerValue'
        upperValue:
          $ref: './upperValue.yaml#/components/schemas/upperValue'
