components:
  schemas:
   versionData:
     description: Versionsangabe eines Objektes, einer Liste oder eines Datensatzes
     required:
      - versionsNumber
      - validFromTime
     type: object
     properties:
      versionsNumber:
        $ref: './versionNumber.yaml#/components/Schemas/versionNumber'
      validFromTime:
        $ref: './validFromTime.yaml#/components/Schemas/validFromTime'
      previousVersion:
        $ref: './versionNumber.yaml#/components/Schemas/versionNumber'
        description: Angabe der Vorgängerversion
