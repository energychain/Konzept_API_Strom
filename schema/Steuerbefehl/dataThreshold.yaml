components:
  schemas:
     dataThreshold:
      required:
        - threshold
        - lowerValue
        - upperValue
      type: object
      properties:
        threshold:
          $ref: '../Schwellwerte/threshold.yaml#/components/Schemas/threshold'
        lowerValue:
          $ref: '../Schwellwerte/lowerValue.yaml#/components/Schemas/lowerValue'
        upperValue:
          $ref: '../Schwellwerte/upperValue.yaml#/components/Schemas/upperValue'
