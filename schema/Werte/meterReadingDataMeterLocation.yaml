components:
  schemas:
    meterReadingDataMeterLocation:
      required:
        - meloId
        - readingDateTime
        - obisCode
        - value
        - valueQuality
        - meterNumber
      type: object
      properties:
        meloId:
          $ref: './meloId.yaml#/components/schemas/meloId'
        meterNumber:
          $ref: './meterNumber.yaml#/components/schemas/meterNumber'
        obisCode:
          $ref: './obisCodeMeterReading.yaml#/components/schemas/obisCodeMeterReading'
        readingDateTime:
          $ref: './readingDateTime.yaml#/components/schemas/dateTime'
        value:
          $ref: './value.yaml#/components/schemas/value'
        valueQuality:
          $ref: './valueQuality.yaml#/components/schemas/valueQuality'
