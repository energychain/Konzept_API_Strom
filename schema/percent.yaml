components: 
  schemas:
    percent:
      pattern: "\\d{1,2}(\\.[\\d]{1,3})?"
      type: number
      description: "Prozentualer Wert, wenn proportion mit percent vorhanden ist."
      example: "75.912"